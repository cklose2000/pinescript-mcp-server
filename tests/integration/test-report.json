{
  "Gold Scalping Strategy": {
    "script": "//@version=6\r\nstrategy(\"Gold Scalping BOS & CHoCH\", overlay=true, default_qty_type=strategy.percent_of_equity, default_qty_value=1)\r\n\r\n// Support & Resistance Levels\r\nrecentLow = ta.lowest(low, 10)\r\nrecentHigh = ta.highest(high, 10)\r\n\r\n// Break of Structure (BOS) with Confirmation\r\nvar float lastSwingHigh = na\r\nvar float lastSwingLow = na\r\nbosBullish = false\r\nbosBearish = false\r\n\r\nlastSwingHigh := ta.highest(high[1], 5)\r\nbosBullish := high > lastSwingHigh\r\n\r\nlastSwingLow := ta.lowest(low[1], 5)\r\nbosBearish := low < lastSwingLow\r\n\r\n// Change of Character (CHoCH) with Immediate Reaction\r\nchochBullish = bosBearish and ta.crossover(close, lastSwingLow)\r\nchochBearish = bosBullish and ta.crossunder(close, lastSwingHigh)\r\n\r\n// Buy Entry Conditions (Refined for Early Entries)\r\nbuyCondition = bosBullish and chochBullish\r\n\r\n// Sell Entry Conditions (Refined for Early Entries)\r\nsellCondition = bosBearish and chochBearish\r\n\r\n// Stop Loss & Take Profit (Dynamic and Adaptive)\r\nlongSL = recentLow\r\nshortSL = recentHigh\r\nlongTP = close + ((close - longSL) * 2)\r\nshortTP = close - ((shortSL - close) * 2)\r\n\r\n// Ensure SL and TP are valid before executing trades\r\nvalidLongTrade = buyCondition and (longSL < close)\r\nvalidShortTrade = sellCondition and (shortSL > close)\r\n\r\n// Execute Trades\r\nif validLongTrade\r\n    strategy.entry(\"Long\", strategy.long)\r\n    strategy.exit(\"Exit Long\", from_entry=\"Long\", stop=longSL, limit=longTP)\r\n\r\nif validShortTrade\r\n    strategy.entry(\"Short\", strategy.short)\r\n    strategy.exit(\"Exit Short\", from_entry=\"Short\", stop=shortSL, limit=shortTP)\r\n\r\n// Plot Buy/Sell Signals\r\nplotshape(series=buyCondition, location=location.belowbar, color=color.green, style=shape.labelup, title=\"BUY\")\r\nplotshape(series=sellCondition, location=location.abovebar, color=color.red, style=shape.labeldown, title=\"SELL\")\r\n\r\n// Debugging: Show swing high/low levels\r\nplot(lastSwingHigh, title=\"Last Swing High\", color=color.blue, linewidth=2, style=plot.style_linebr)\r\nplot(lastSwingLow, title=\"Last Swing Low\", color=color.orange, linewidth=2, style=plot.style_linebr)",
    "metrics": {
      "versionDetection": 0.042600035667419434,
      "validation": 2.1444000005722046,
      "formattingDefault": 1.7265999913215637,
      "formattingMinimal": 0.2336999773979187,
      "downgradeToV5": 0.3628000020980835,
      "fixMissingVersion": 1.7473999857902527,
      "fixUnbalancedParentheses": 0.39660000801086426
    },
    "validationResults": {
      "valid": true,
      "errors": [],
      "warnings": [
        "Script uses version v6, but validator was called with v5",
        "Line 2: Possible missing comma in function call to 'strategy'",
        "Line 43: Possible missing comma in function call to 'exit'",
        "Line 47: Possible missing comma in function call to 'exit'",
        "Line 9: In v6, consider using 'let' for non-mutable variables and 'var' for mutable ones with ':='",
        "Line 10: In v6, consider using 'let' for non-mutable variables and 'var' for mutable ones with ':='",
        "Line 42: In v6, consider using named parameters for clarity (e.g., 'param_name = value')",
        "Line 46: In v6, consider using named parameters for clarity (e.g., 'param_name = value')",
        "PineScript v6 encourages using imports for standard libraries (e.g., 'import ta')"
      ]
    },
    "formattingResults": {
      "default": {
        "formatted": "//@version=6\r\nstrategy(\"Gold Scalping BOS & CHoCH\", overlay = true, default_qty_type = strategy.percent_of_equity, default_qty_value = 1)\r\n\r\n// Support & Resistance Levels\r\nrecentLow = ta.lowest(low, 10)\r\nrecentHigh = ta.highest(high, 10)\r\n\r\n// Break of Structure (BOS) with Confirmation\r\nvar float lastSwingHigh = na\r\nvar float lastSwingLow = na\r\nbosBullish = false\r\nbosBearish = false\r\n\r\nlastSwingHigh := ta.highest(high[1], 5)\r\nbosBullish := high > lastSwingHigh\r\n\r\nlastSwingLow := ta.lowest(low[1], 5)\r\nbosBearish := low < lastSwingLow\r\n\r\n// Change of Character (CHoCH) with Immediate Reaction\r\nchochBullish = bosBearish and ta.crossover(close, lastSwingLow)\r\nchochBearish = bosBullish and ta.crossunder(close, lastSwingHigh)\r\n\r\n// Buy Entry Conditions (Refined for Early Entries)\r\nbuyCondition = bosBullish and chochBullish\r\n\r\n// Sell Entry Conditions (Refined for Early Entries)\r\nsellCondition = bosBearish and chochBearish\r\n\r\n// Stop Loss & Take Profit (Dynamic and Adaptive)\r\nlongSL = recentLow\r\nshortSL = recentHigh\r\nlongTP = close + ((close - longSL) * 2)\r\nshortTP = close - ((shortSL - close) * 2)\r\n\r\n// Ensure SL and TP are valid before executing trades\r\nvalidLongTrade = buyCondition and (longSL < close)\r\nvalidShortTrade = sellCondition and (shortSL > close)\r\n\r\n// Execute Trades\r\nif validLongTrade\r\n strategy.entry(\"Long\", strategy.long)\r\n strategy.exit(\"Exit Long\", from_entry = \"Long\", stop = longSL, limit = longTP)\r\n\r\n if validShortTrade\r\n strategy.entry(\"Short\", strategy.short)\r\n strategy.exit(\"Exit Short\", from_entry = \"Short\", stop = shortSL, limit = shortTP)\r\n\r\n// Plot Buy/Sell Signals\r\n plotshape(series = buyCondition, location = location.belowbar, color = color.green, style = shape.labelup, title = \"BUY\")\r\n plotshape(series = sellCondition, location = location.abovebar, color = color.red, style = shape.labeldown, title = \"SELL\")\r\n\r\n// Debugging: Show swing high/low levels\r\n plot(lastSwingHigh, title = \"Last Swing High\", color = color.blue, linewidth = 2, style = plot.style_linebr)\r\n plot(lastSwingLow, title = \"Last Swing Low\", color = color.orange, linewidth = 2, style = plot.style_linebr)",
        "changes": [
          "Standardized indentation",
          "Standardized spacing around operators and commas"
        ],
        "warnings": [
          "Line 2 exceeds maximum line length of 80 characters",
          "Line 47 exceeds maximum line length of 80 characters",
          "Line 50 exceeds maximum line length of 80 characters",
          "Line 51 exceeds maximum line length of 80 characters",
          "Line 54 exceeds maximum line length of 80 characters",
          "Line 55 exceeds maximum line length of 80 characters"
        ]
      },
      "minimal": {
        "formatted": "//@version=6\r\nstrategy(\"Gold Scalping BOS & CHoCH\", overlay=true, default_qty_type=strategy.percent_of_equity, default_qty_value=1)\r\n\n// Support & Resistance Levels\r\nrecentLow = ta.lowest(low, 10)\r\nrecentHigh = ta.highest(high, 10)\r\n\n// Break of Structure (BOS) with Confirmation\r\nvar float lastSwingHigh = na\r\nvar float lastSwingLow = na\r\nbosBullish = false\r\nbosBearish = false\r\n\nlastSwingHigh := ta.highest(high[1], 5)\r\nbosBullish := high > lastSwingHigh\r\n\nlastSwingLow := ta.lowest(low[1], 5)\r\nbosBearish := low < lastSwingLow\r\n\n// Change of Character (CHoCH) with Immediate Reaction\r\nchochBullish = bosBearish and ta.crossover(close, lastSwingLow)\r\nchochBearish = bosBullish and ta.crossunder(close, lastSwingHigh)\r\n\n// Buy Entry Conditions (Refined for Early Entries)\r\nbuyCondition = bosBullish and chochBullish\r\n\n// Sell Entry Conditions (Refined for Early Entries)\r\nsellCondition = bosBearish and chochBearish\r\n\n// Stop Loss & Take Profit (Dynamic and Adaptive)\r\nlongSL = recentLow\r\nshortSL = recentHigh\r\nlongTP = close + ((close - longSL) * 2)\r\nshortTP = close - ((shortSL - close) * 2)\r\n\n// Ensure SL and TP are valid before executing trades\r\nvalidLongTrade = buyCondition and (longSL < close)\r\nvalidShortTrade = sellCondition and (shortSL > close)\r\n\n// Execute Trades\r\nif validLongTrade\r\n\tstrategy.entry(\"Long\", strategy.long)\r\n\tstrategy.exit(\"Exit Long\", from_entry=\"Long\", stop=longSL, limit=longTP)\r\n\n\tif validShortTrade\r\n\t\tstrategy.entry(\"Short\", strategy.short)\r\n\t\tstrategy.exit(\"Exit Short\", from_entry=\"Short\", stop=shortSL, limit=shortTP)\r\n\n// Plot Buy/Sell Signals\r\n\t\tplotshape(series=buyCondition, location=location.belowbar, color=color.green, style=shape.labelup, title=\"BUY\")\r\n\t\tplotshape(series=sellCondition, location=location.abovebar, color=color.red, style=shape.labeldown, title=\"SELL\")\r\n\n// Debugging: Show swing high/low levels\r\n\t\tplot(lastSwingHigh, title=\"Last Swing High\", color=color.blue, linewidth=2, style=plot.style_linebr)\r\n\t\tplot(lastSwingLow, title=\"Last Swing Low\", color=color.orange, linewidth=2, style=plot.style_linebr)",
        "changes": [
          "Standardized indentation"
        ],
        "warnings": []
      }
    },
    "versionResults": {
      "downgradeToV5": "//@version=5\r\nstrategy(\"Gold Scalping BOS & CHoCH\", overlay=true, default_qty_type=strategy.percent_of_equity, default_qty_value=1)\r\n\r\n// Support & Resistance Levels\r\nrecentLow = ta.lowest(low, 10)\r\nrecentHigh = ta.highest(high, 10)\r\n\r\n// Break of Structure (BOS) with Confirmation\r\nvar float lastSwingHigh = na\r\nvar float lastSwingLow = na\r\nbosBullish = false\r\nbosBearish = false\r\n\r\nlastSwingHigh := ta.highest(high[1], 5)\r\nbosBullish := high > lastSwingHigh\r\n\r\nlastSwingLow := ta.lowest(low[1], 5)\r\nbosBearish := low < lastSwingLow\r\n\r\n// Change of Character (CHoCH) with Immediate Reaction\r\nchochBullish = bosBearish and ta.crossover(close, lastSwingLow)\r\nchochBearish = bosBullish and ta.crossunder(close, lastSwingHigh)\r\n\r\n// Buy Entry Conditions (Refined for Early Entries)\r\nbuyCondition = bosBullish and chochBullish\r\n\r\n// Sell Entry Conditions (Refined for Early Entries)\r\nsellCondition = bosBearish and chochBearish\r\n\r\n// Stop Loss & Take Profit (Dynamic and Adaptive)\r\nlongSL = recentLow\r\nshortSL = recentHigh\r\nlongTP = close + ((close - longSL) * 2)\r\nshortTP = close - ((shortSL - close) * 2)\r\n\r\n// Ensure SL and TP are valid before executing trades\r\nvalidLongTrade = buyCondition and (longSL < close)\r\nvalidShortTrade = sellCondition and (shortSL > close)\r\n\r\n// Execute Trades\r\nif validLongTrade\r\n    strategy.entry(\"Long\", strategy.long)\r\n    strategy.exit(\"Exit Long\", from_entry=\"Long\", stop=longSL, limit=longTP)\r\n\r\nif validShortTrade\r\n    strategy.entry(\"Short\", strategy.short)\r\n    strategy.exit(\"Exit Short\", from_entry=\"Short\", stop=shortSL, limit=shortTP)\r\n\r\n// Plot Buy/Sell Signals\r\nplotshape(series=buyCondition, location=location.belowbar, color=color.green, style=shape.labelup, title=\"BUY\")\r\nplotshape(series=sellCondition, location=location.abovebar, color=color.red, style=shape.labeldown, title=\"SELL\")\r\n\r\n// Debugging: Show swing high/low levels\r\nplot(lastSwingHigh, title=\"Last Swing High\", color=color.blue, linewidth=2, style=plot.style_linebr)\r\nplot(lastSwingLow, title=\"Last Swing Low\", color=color.orange, linewidth=2, style=plot.style_linebr)"
    },
    "fixerResults": {
      "missingVersion": {
        "fixed": true,
        "script": "//@version=5\n\r\nstrategy(\"Gold Scalping BOS & CHoCH\"), overlay=true), default_qty_type=strategy.percent_of_equity, default_qty_value=1)\r\n\r\n// Support & Resistance Levels\r\nrecentLow = ta.lowest(low, 10)\r\nrecentHigh = ta.highest(high, 10)\r\n\r\n// Break of Structure (BOS) with Confirmation\r\nvar float lastSwingHigh = na\r\nvar float lastSwingLow = na\r\nbosBullish = false\r\nbosBearish = false\r\n\r\nlastSwingHigh := ta.highest(high[1], 5)\r\nbosBullish := high > lastSwingHigh\r\n\r\nlastSwingLow := ta.lowest(low[1], 5)\r\nbosBearish := low < lastSwingLow\r\n\r\n// Change of Character (CHoCH) with Immediate Reaction\r\nchochBullish = bosBearish and ta.crossover(close, lastSwingLow)\r\nchochBearish = bosBullish and ta.crossunder(close, lastSwingHigh)\r\n\r\n// Buy Entry Conditions (Refined for Early Entries)\r\nbuyCondition = bosBullish and chochBullish\r\n\r\n// Sell Entry Conditions (Refined for Early Entries)\r\nsellCondition = bosBearish and chochBearish\r\n\r\n// Stop Loss & Take Profit (Dynamic and Adaptive)\r\nlongSL = recentLow\r\nshortSL = recentHigh\r\nlongTP = close + ((close - longSL) * 2)\r\nshortTP = close - ((shortSL - close) * 2)\r\n\r\n// Ensure SL and TP are valid before executing trades\r\nvalidLongTrade = buyCondition and (longSL < close)\r\nvalidShortTrade = sellCondition and (shortSL > close)\r\n\r\n// Execute Trades\r\nif validLongTrade\r\n    strategy.entry(\"Long\", strategy.long)\r\n    strategy.exit(\"Exit Long\", from_entry=\"Long\", stop=longSL, limit=longTP)\r\n\r\nif validShortTrade\r\n    strategy.entry(\"Short\", strategy.short)\r\n    strategy.exit(\"Exit Short\", from_entry=\"Short\", stop=shortSL, limit=shortTP)\r\n\r\n// Plot Buy/Sell Signals\r\nplotshape(series=buyCondition, location=location.belowbar, color=color.green, style=shape.labelup, title=\"BUY\")\r\nplotshape(series=sellCondition, location=location.abovebar, color=color.red, style=shape.labeldown, title=\"SELL\")\r\n\r\n// Debugging: Show swing high/low levels\r\nplot(lastSwingHigh, title=\"Last Swing High\", color=color.blue, linewidth=2, style=plot.style_linebr)\r\nplot(lastSwingLow, title=\"Last Swing Low\", color=color.orange, linewidth=2, style=plot.style_linebr)",
        "changes": [
          "Added missing version annotation (@version=5)",
          "Fixed unbalanced parentheses in function call matching pattern: /(strategy|indicator)\\s*\\(\\s*(\"[^\"]*\")\\s*,\\s*overlay\\s*=\\s*true(?!\\))/",
          "Fixed unbalanced parentheses in function call matching pattern: /(strategy|indicator)\\s*\\(\\s*(\"[^\"]*\")(?!\\))/"
        ]
      },
      "unbalancedParentheses": {
        "fixed": true,
        "script": "//@version=6\r\nstrategy(\"Gold Scalping BOS & CHoCH\"), overlay=true), default_qty_type=strategy.percent_of_equity, default_qty_value=1)\r\n\r\n// Support & Resistance Levels\r\nrecentLow = ta.lowest(low, 10)\r\nrecentHigh = ta.highest(high, 10)\r\n\r\n// Break of Structure (BOS) with Confirmation\r\nvar float lastSwingHigh = na\r\nvar float lastSwingLow = na\r\nbosBullish = false\r\nbosBearish = false\r\n\r\nlastSwingHigh := ta.highest(high[1], 5)\r\nbosBullish := high > lastSwingHigh\r\n\r\nlastSwingLow := ta.lowest(low[1], 5)\r\nbosBearish := low < lastSwingLow\r\n\r\n// Change of Character (CHoCH) with Immediate Reaction\r\nchochBullish = bosBearish and ta.crossover(close, lastSwingLow)\r\nchochBearish = bosBullish and ta.crossunder(close, lastSwingHigh)\r\n\r\n// Buy Entry Conditions (Refined for Early Entries)\r\nbuyCondition = bosBullish and chochBullish\r\n\r\n// Sell Entry Conditions (Refined for Early Entries)\r\nsellCondition = bosBearish and chochBearish\r\n\r\n// Stop Loss & Take Profit (Dynamic and Adaptive)\r\nlongSL = recentLow\r\nshortSL = recentHigh\r\nlongTP = close + ((close - longSL) * 2)\r\nshortTP = close - ((shortSL - close) * 2)\r\n\r\n// Ensure SL and TP are valid before executing trades\r\nvalidLongTrade = buyCondition and (longSL < close)\r\nvalidShortTrade = sellCondition and (shortSL > close)\r\n\r\n// Execute Trades\r\nif validLongTrade\r\n    strategy.entry(\"Long\", strategy.long)\r\n    strategy.exit(\"Exit Long\", from_entry=\"Long\", stop=longSL, limit=longTP)\r\n\r\nif validShortTrade\r\n    strategy.entry(\"Short\", strategy.short)\r\n    strategy.exit(\"Exit Short\", from_entry=\"Short\", stop=shortSL, limit=shortTP)\r\n\r\n// Plot Buy/Sell Signals\r\nplotshape(series=buyCondition, location=location.belowbar, color=color.green, style=shape.labelup, title=\"BUY\")\r\nplotshape(series=sellCondition, location=location.abovebar, color=color.red, style=shape.labeldown, title=\"SELL\")\r\n\r\n// Debugging: Show swing high/low levels\r\nplot(lastSwingHigh, title=\"Last Swing High\", color=color.blue, linewidth=2, style=plot.style_linebr)\r\nplot(lastSwingLow, title=\"Last Swing Low\", color=color.orange, linewidth=2, style=plot.style_linebr)",
        "changes": [
          "Fixed unbalanced parentheses in function call matching pattern: /(strategy|indicator)\\s*\\(\\s*(\"[^\"]*\")\\s*,\\s*overlay\\s*=\\s*true(?!\\))/",
          "Fixed unbalanced parentheses in function call matching pattern: /(strategy|indicator)\\s*\\(\\s*(\"[^\"]*\")(?!\\))/"
        ]
      }
    },
    "detectedVersion": "v6"
  }
}